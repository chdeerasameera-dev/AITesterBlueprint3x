# Dependencies
node_modules/

# Environment — never commit real credentials
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.log

# Temp files
.tmp/
*.tmp

# Vite cache
.vite/

# Build cache
.cache/

# NOTE: dist/ is intentionally NOT ignored
# Vercel needs the pre-built frontend committed to the repo
